import { getMainFolder } from "@/actions/folder";
import { createOrFindUser } from "@/actions/user";
import useFileSelectionStore from "@/hooks/useFileSelectionStore";

const Test = async ({
  searhchParams,
}: {
  searhchParams: { folderId: string };
}) => {
  return <div className="text-white"></div>;
};

export default Test;
